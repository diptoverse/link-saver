# Link Saver

Personal link-saving app — paste a URL, auto-extract platform/title/creator, rate priority 1–5.

## Stack

Vite + React + Tailwind + Supabase + Vercel

## Local dev

```bash
npm install
cp .env.example .env   # fill in your Supabase keys
npm run dev
```
